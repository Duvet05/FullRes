{
    "name": "fullres",
    "version": "1.0.0",
    "description": "Chromium extension to force max resolution on streaming platforms",
    "scripts": {
        "build": "tsc && cp manifest.json dist/ && cp -r assets dist/",
        "watch": "tsc --watch"
    },
    "devDependencies": {
        "@types/chrome": "^0.0.315",
        "@types/filewriter": "^0.0.33",
        "@types/har-format": "^1.2.16",
        "typescript": "^5.5.4"
    },
    "license": "MIT"
}
